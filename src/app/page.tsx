export default function Home() {
  const hnald = async () => {
    
  };

  return (
    <>
      <a href={`${process.env.NEXT_PUBLIC_API_HOST}/account/redirect-login/google`}>login</a>
    </>
  );
}
